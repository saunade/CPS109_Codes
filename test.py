def count(num):#num is the variable name
    print(num + 1) #num prints out to user with an added one value

if __name__ == "__main__": #main function that we call our defined functions
    for n in range(10): 
        count(n) #makes the value of num n (the counter in the for loop)